language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wl-cordova' ]; then cd .. && eval "mv $currentfolder generator-wl-cordova" && cd generator-wl-cordova; fi

